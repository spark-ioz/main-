#include <stdio.h>

int main()
{
    char *instr[] = {
        "ADD R1,R2,R3",
        "SUB R4,R5,R6",
        "MUL R7,R8,R9"
    };

    char *stage[] = {"IF", "ID", "EX", "MEM", "WB"};

    int cycle, i, s;

    printf("Instructions:\n");
    for (i = 0; i < 3; i++)
        printf("I%d : %s\n", i + 1, instr[i]);

    printf("\nPipeline Execution:\n\n");

    printf("Clock Cycle\tI1\tI2\tI3\n");

    for (cycle = 1; cycle <= 7